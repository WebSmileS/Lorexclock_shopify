<!-- /templates/test-accordion.liquid -->

        <header class="section-header text-center">
            <h1>{{ page.title }}</h1>
        </header>
        {% section 'test-accordion.liquid' %}
        {% section 'test-banner.liquid' %}
        {% section 'test-small-banner.liquid' %}
        {% section 'test-featured-collection.liquid' %}
